info:
  name: 16_Order-NullQuantity
  type: http
  seq: 16

http:
  method: POST
  url: "{{URL}}/orders"
  body:
    type: json
    data: |-
      {
        "itemId": 2,
        "quantity": null
      }
  auth:
    type: bearer
    token: "{{jwt_token}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        test("Status code should be 400 or 500", function() {
          expect(res.getStatus()).to.be.oneOf([400, 500]);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
